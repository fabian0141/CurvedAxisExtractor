{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile C Extension",
            "type": "shell",
            "command": "python setup.py build; python setup.py install",
            "group": "test",
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            },
            "options": {
                "cwd": "${workspaceFolder}/extension"
            }
        }
    ]
}